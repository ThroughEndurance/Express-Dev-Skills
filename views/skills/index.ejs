<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kiefer's Skills</title>
</head>
 <body>
   <h1>Kiefer's Skills</h1>
   <ul>
     <% skills.forEach(function(skill) { %>
       <li>
         <%= skill.skill %>
           - 
         <%= skill.done ? 'done' : 'not done' %>
       </li>
     <% }); %>
   </ul>
 </body>
</html>